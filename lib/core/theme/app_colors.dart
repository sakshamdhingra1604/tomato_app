import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryYellow = Color(0xFFFFD200); // Vibrant Tomato/Food Yellow
  static const Color secondaryOrange = Color(0xFFFF9100);

  // Background Colors (Dark Glassmorphism)
  static const Color scaffoldBg = Color(0xFF0F0F0F);
  static const Color cardBg = Color(0xFF1E1E1E);
  static const Color surfaceBg = Color(0xFF252525);

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.grey;

  // Accents
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
}