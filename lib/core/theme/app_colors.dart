import 'package:flutter/material.dart';

class AppColors {
  // --- EXISTING NAMES (No errors in your files) ---
  static const Color primaryYellow = Color(0xFFFFD200);
  static const Color secondaryOrange = Color(0xFFFF9100);
  static const Color scaffoldBg = Color(0xFF0F0F0F); // Dark Mode Default
  static const Color cardBg = Color(0xFF1E1E1E);     // Dark Mode Default
  static const Color surfaceBg = Color(0xFF252525);  // Dark Mode Default
  static const Color textPrimary = Colors.white;     // Dark Mode Default
  static const Color textSecondary = Colors.white70; // Dark Mode Default
  static const Color textMuted = Colors.grey;
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);

  // --- NEW LIGHT MODE COLORS (For the Theme Engine) ---
  static const Color lightScaffoldBg = Color(0xFFF8F9FA);
  static const Color lightCardBg = Colors.white;
  static const Color lightSurfaceBg = Color(0xFFEEEEEE);
  static const Color lightTextPrimary = Color(0xFF1A1A1A);
  static const Color lightTextSecondary = Color(0xFF757575);
}